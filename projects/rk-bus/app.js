const buses=[['RK Express','06:30','13:45','₹699'],['VRK Travels','08:15','15:30','₹799'],['South Connect','21:00','05:45','₹899']];const $=id=>document.getElementById(id);function render(){const from=$('from').value||'Origin',to=$('to').value||'Destination';$('results').innerHTML=`<h2>${from} → ${to}</h2>`+buses.map(b=>`<article class="bus"><div><h3>${b[0]}</h3><p>${b[1]} → ${b[2]}</p><strong>${b[3]}</strong></div><button onclick="alert('Seat selection demo for ${b[0]}')">Select Seats</button></article>`).join('')}$('find').addEventListener('click',render);render();
